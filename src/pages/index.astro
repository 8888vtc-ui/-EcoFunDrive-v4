---
// ═══════════════════════════════════════════════════════════
// ECOFUNDRIVE V3.0 - PAGE D'ACCUEIL
// Page d'accueil dynamique avec génération IA
// ═══════════════════════════════════════════════════════════

import PageLayout from '../layouts/PageLayout.astro';
import Hero from '../components/Hero.astro';
import CTA from '../components/CTA.astro';
import { siteConfig } from '../lib/config';

// SEO Configuration for the generator landing page
const title = 'Générateur de Sites SEO Automatique';
const metaTitle = 'Générateur de Sites SEO Automatique | Création & Déploiement Netlify';
const metaDescription = 'Créez des sites complets optimisés SEO avec contenu IA, maillage interne et déploiement Netlify automatique à partir d\'un simple formulaire.';
const lang = 'fr';
const canonical = siteConfig.siteUrl;
const ogImage = '/images/hero/default-hero.webp';
---

<PageLayout
  title={title}
  metaTitle={metaTitle}
  metaDescription={metaDescription}
  lang={lang}
  canonical={canonical}
  ogImage={ogImage}
>
  <!-- Hero Section -->
  <Hero
    title="Générez des sites SEO complets en quelques minutes"
    subtitle="Formulaire → mots-clés → contenu IA → maillage interne → déploiement Netlify. Un seul outil pour lancer vos sites rapidement."
    image="/images/hero/default-hero.webp"
    showCTA={true}
    ctaText="Ouvrir le générateur"
    ctaLink="/generator"
  />

  <!-- Features Section -->
  <section class="services-section">
    <div class="container">
      <div class="section-header">
        <h2>Générateur de Sites SEO Clé en Main</h2>
        <p>Du formulaire au site déployé, avec contrôles SEO et contenus IA multi-passes.</p>
      </div>

      <div class="services-grid">
        <div class="service-card">
          <div class="service-icon">🧩</div>
          <h3>Formulaire structuré</h3>
          <p>Identité, RGPD, activité, langues, tracking, affiliation, réseaux sociaux, avis : tout est pris en compte.</p>
        </div>

        <div class="service-card">
          <div class="service-icon">📈</div>
          <h3>SEO multi-couches</h3>
          <p>Mots-clés low competition, clustering 1+4, audit SEO technique + IA, maillage interne automatique.</p>
        </div>

        <div class="service-card">
          <div class="service-icon">🚀</div>
          <h3>Déploiement Netlify</h3>
          <p>Build Astro, déploiement automatique, mode dry-run, rapports complets de génération et de mise à jour.</p>
        </div>

        <div class="service-card">
          <div class="service-icon">🤖</div>
          <h3>Chatbot & SEO pour IA</h3>
          <p>Résumé structuré, base de connaissances, scénarios de chatbot et contenus optimisés pour les IA.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Main CTA -->
  <CTA
    title="Prêt à générer votre prochain site ?"
    subtitle="Lancez un premier test en dry-run avec une seule page, puis scalez jusqu'à des centaines de pages."
    primaryText="Accéder au générateur"
    primaryLink="/generator"
    secondaryText="Voir la documentation"
    secondaryLink="/docs"
    variant="gradient"
    size="large"
  />
</PageLayout>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-header h2 {
    font-size: clamp(2rem, 4vw, 2.5rem);
    font-weight: 700;
    margin-bottom: 1rem;
    color: #333;
  }

  .section-header p {
    font-size: 1.2rem;
    color: #666;
    max-width: 600px;
    margin: 0 auto;
  }

  /* Services Section */
  .services-section {
    padding: 5rem 0;
    background: #f8f9fa;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
  }

  .service-card {
    background: white;
    padding: 2.5rem 2rem;
    border-radius: 16px;
    text-align: center;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .service-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
  }

  .service-icon {
    font-size: 3rem;
    margin-bottom: 1.5rem;
  }

  .service-card h3 {
    font-size: 1.3rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: #333;
  }

  .service-card p {
    color: #666;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .service-link {
    color: #007bff;
    text-decoration: none;
    font-weight: 600;
    transition: color 0.3s ease;
  }

  .service-link:hover {
    color: #0056b3;
  }

  /* Fleet Section */
  .fleet-section {
    padding: 5rem 0;
    background: white;
  }

  .fleet-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .fleet-card {
    background: #f8f9fa;
    border-radius: 16px;
    overflow: hidden;
    transition: transform 0.3s ease;
  }

  .fleet-card:hover {
    transform: translateY(-5px);
  }

  .fleet-image {
    height: 200px;
    overflow: hidden;
  }

  .fleet-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .fleet-info {
    padding: 1.5rem;
  }

  .fleet-info h3 {
    font-size: 1.3rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: #333;
  }

  .fleet-specs {
    display: flex;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .spec {
    background: white;
    padding: 0.5rem 1rem;
    border-radius: 8px;
    font-size: 0.9rem;
    color: #666;
  }

  .fleet-price {
    display: flex;
    align-items: baseline;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }

  .price {
    font-size: 1.5rem;
    font-weight: 700;
    color: #007bff;
  }

  .price-label {
    color: #666;
    font-size: 0.9rem;
  }

  .fleet-feature {
    background: #007bff;
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 8px;
    font-size: 0.9rem;
    text-align: center;
  }

  /* Pricing Preview */
  .pricing-preview {
    padding: 5rem 0;
    background: #f8f9fa;
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .pricing-card {
    background: white;
    padding: 2rem;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
  }

  .pricing-card h3 {
    font-size: 1.3rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: #333;
  }

  .route-info {
    margin-bottom: 1.5rem;
  }

  .distance {
    color: #666;
    font-size: 0.9rem;
  }

  .price-options {
    display: flex;
    gap: 1rem;
  }

  .price-option {
    flex: 1;
    text-align: center;
    padding: 1rem;
    background: #f8f9fa;
    border-radius: 8px;
  }

  .model {
    display: block;
    font-size: 0.9rem;
    color: #666;
    margin-bottom: 0.5rem;
  }

  .price-option .price {
    font-size: 1.3rem;
    font-weight: 700;
    color: #007bff;
  }

  .pricing-cta {
    text-align: center;
  }

  .cta-button {
    background: #007bff;
    color: white;
    padding: 1rem 2rem;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .cta-button:hover {
    background: #0056b3;
    transform: translateY(-2px);
  }

  /* Trust Section */
  .trust-section {
    padding: 5rem 0;
    background: white;
  }

  .trust-content {
    text-align: center;
  }

  .trust-text h2 {
    margin-bottom: 1rem;
  }

  .trust-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .trust-item {
    text-align: center;
  }

  .trust-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .trust-item h4 {
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: #333;
  }

  .trust-item p {
    color: #666;
    font-size: 0.9rem;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .container {
      padding: 0 1rem;
    }

    .services-grid,
    .fleet-grid,
    .pricing-grid {
      grid-template-columns: 1fr;
    }

    .trust-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .price-options {
      flex-direction: column;
    }
  }

  @media (max-width: 480px) {
    .trust-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
